function boxBlur(image) {

    /* On va d'abord créer un tableau vide nommée arrSums dans lequel nous allons pusher notre valeur à la fin de chaque itération */
    let arrSums = []; 
    let arrLine = []; // on va y pusher un array vide dans lequel nous allons créer un autre array vide pour y pusher les valeurs et quand 

}
  
boxBlur([[7, 4, 0, 1],
         [5, 6, 2, 2],
         [6, 10, 7, 8],
         [1, 4, 2, 0]])